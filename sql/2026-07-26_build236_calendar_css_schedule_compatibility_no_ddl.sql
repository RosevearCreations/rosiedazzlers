-- Build 236: no DDL required.
-- Restores shared CSS, repairs legacy date_blocks(blocked_date)/slot_blocks(blocked_date,slot) query compatibility,
-- and improves the Block Calendar/launch-readiness interfaces.
select 'Build 236 requires no database schema change' as note;
