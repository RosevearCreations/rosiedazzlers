name: Build 303 Development Runtime Acceptance

on:
  push:
    branches: [dev]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Validate exact Build 303 source
        run: python scripts/build303_release_check.py
      - name: Validate deployed tax-support surface read-only
        run: bash scripts/build303_http_smoke.sh https://dev.rosiedazzlers.pages.dev
