name: Build 313 Development Runtime Acceptance

on:
  push:
    branches: [dev]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Validate exact Build 313 source contract
        run: python scripts/build313_release_check.py
      - name: Validate Development Product Catalog runtime read-only
        run: bash scripts/build313_http_smoke.sh https://dev.rosiedazzlers.pages.dev
