const esc=(v)=>String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export async function mount({host,api}){
 let visible=true;host.innerHTML='<section class="panel stack"><div class="module-head"><div><span class="badge">Manual module</span><h2>Blocked dates & slots</h2></div><button class="btn ghost" id="opsBlocksRefresh">Refresh now</button></div><div id="opsBlocksBody"><div class="notice">Loading once…</div></div><a class="btn ghost small" href="/admin-blocks.html">Open block calendar</a></section>';
 const body=()=>host.querySelector('#opsBlocksBody');async function load(){if(!visible)return;const out=await api.requestJson('/api/admin/blocks',{method:'POST',body:'{}'});const dates=(out.date_blocks||[]).slice(0,20),slots=(out.slot_blocks||[]).slice(0,30);body().innerHTML=`<div class="operations-stats"><div><span>Date blocks</span><strong>${(out.date_blocks||[]).length}</strong></div><div><span>Slot blocks</span><strong>${(out.slot_blocks||[]).length}</strong></div></div><div class="operations-list">${dates.map((x)=>`<article class="operations-row"><div><h3>${esc(x.blocked_date)}</h3><p class="mini">Whole day · ${esc(x.reason||'No reason entered')}</p></div></article>`).join('')}${slots.map((x)=>`<article class="operations-row"><div><h3>${esc(x.blocked_date)} · ${esc(x.slot)}</h3><p class="mini">Slot block · ${esc(x.reason||'No reason entered')}</p></div></article>`).join('')}</div>`;}
 host.querySelector('#opsBlocksRefresh').addEventListener('click',()=>load().catch((e)=>body().innerHTML=`<div class="notice bad">${esc(e.message)}</div>`));await load();return{suspend(){visible=false;},setVisibility(v){visible=v!==false;}};
}
