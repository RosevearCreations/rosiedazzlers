// Build 265 staff module launcher: auth only, no subsystem data loads.
(function(globalScope){'use strict';const $=(id)=>document.getElementById(id);const esc=(v)=>String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function boot(){await globalScope.AdminShell.boot({pageKey:'app-launcher',onReady:async({actor})=>{const resolver=globalScope.RosieAppCore.ModuleResolver;const modules=resolver.allowed(actor);if(modules.length===1){resolver.remember(modules[0].key);location.replace(modules[0].href);return;}$('launcherStatus').className='notice ok';$('launcherStatus').textContent=modules.length?`Signed in as ${actor.full_name||actor.email||'staff'}. Choose an authorized app.`:'No internal application module is assigned to this staff account.';$('moduleCards').innerHTML=modules.map((m)=>`<article class="panel app-card"><span class="badge">${esc(m.status==='runtime'?'Active modular runtime':'Bridge shell')}</span><h2>${esc(m.name)}</h2><p class="mini">${m.key==='detailer'?'Mobile field workflow; idle when no open job.':m.key==='operations'?'Operational tools load only after you choose a workflow.':'Back-office tools remain on-demand.'}</p><a class="btn primary" href="${esc(m.href)}" data-module="${esc(m.key)}">Open ${esc(m.name)}</a></article>`).join('');$('moduleCards').querySelectorAll('[data-module]').forEach((a)=>a.addEventListener('click',()=>resolver.remember(a.dataset.module)));}});}boot().catch((e)=>{$('launcherStatus').className='notice bad';$('launcherStatus').textContent=e.message||'Could not start app launcher.';});})(window);
