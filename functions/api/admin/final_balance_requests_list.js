// Build 217 — list final-balance requests for staff without returning token hashes.
import { requireStaffAccess, json, serviceHeaders } from "../_lib/staff-auth.js";

const SELECT = ["id","booking_id","customer_name","customer_email","amount_cents","currency","status","notes","payment_url","checkout_url","provider","provider_status","external_checkout_id","checkout_created_at","expires_at","access_token_rotated_at","link_sent_at","link_sent_count","cancelled_at","cancelled_reason","paid_at","paid_amount_cents","provider_payment_intent_id","provider_event_id","created_at","updated_at"].join(",");
export async function onRequestPost({request,env}){return handle({request,env,body:await request.json().catch(()=>({}))})} export async function onRequestGet({request,env}){return handle({request,env,body:Object.fromEntries(new URL(request.url).searchParams.entries())})}
async function handle({request,env,body}){try{const access=await requireStaffAccess({request,env,body,capability:"manage_bookings",allowLegacyAdminFallback:true});if(!access.ok)return access.response;if(!hasSupabaseConfig(env))return json({ok:true,table_ready:false,requests:[],warning:"Supabase not configured."});const res=await fetch(`${env.SUPABASE_URL}/rest/v1/final_balance_payment_requests?select=${encodeURIComponent(SELECT)}&order=created_at.desc&limit=${Math.min(200,Number(body.limit||100)||100)}`,{headers:serviceHeaders(env)});const text=await res.text();const data=safeJson(text);if(!res.ok)return json({ok:true,table_ready:false,requests:[],warning:data?.message||text||"final_balance_payment_requests table is not ready."});return json({ok:true,table_ready:true,requests:Array.isArray(data)?data:[]});}catch(err){return json({ok:false,error:err?.message||"Could not list final balance requests."},500)}} function hasSupabaseConfig(env){return !!(env?.SUPABASE_URL&&(env?.SUPABASE_SERVICE_ROLE_KEY||env?.SUPABASE_SERVICE_KEY||env?.SUPABASE_SERVICE_ROLE||env?.SUPABASE_SECRET_KEY));} function safeJson(t){try{return JSON.parse(t)}catch{return null}}
