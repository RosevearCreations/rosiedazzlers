export async function onRequestPost({ request, env }) { try { if (!env.SUPABASE_URL || !env.SUPABASE_SERVICE_ROLE_KEY) return json({ error: 'Server configuration is incomplete.' }, 500); const adminPassword = request.headers.get('x-admin-password') || ''; if (!env.ADMIN_PASSWORD || adminPassword !== env.ADMIN_PASSWORD) return json({ error: 'Unauthorized.' }, 401); const body = await request.json().catch(() => null); const payload = { item_key: String(body?.item_key || '').trim(), item_type: String(body?.item_type || '').trim().toLowerCase(), name: String(body?.name || '').trim(), category: String(body?.category || '').trim() || null, image_url: String(body?.image_url || '').trim() || null, amazon_url: String(body?.amazon_url || '').trim() || null, qty_on_hand: Number(body?.qty_on_hand || 0), reorder_point: Number(body?.reorder_point || 0), reorder_qty: Number(body?.reorder_qty || 0), unit_label: String(body?.unit_label || '').trim() || null, rating_value: body?.rating_value == null || body?.rating_value === '' ? null : Number(body.rating_value), rating_count: Number(body?.rating_count || 0), preferred_vendor: String(body?.preferred_vendor || '').trim() || null, notes: String(body?.notes || '').trim() || null, is_public: body?.is_public !== false, is_active: body?.is_active !== false, updated_at: new Date().toISOString() }; if (!payload.item_key || !payload.name || !['tool','consumable'].includes(payload.item_type)) return json({ error: 'Missing required fields.' }, 400); const res = await fetch(`${env.SUPABASE_URL}/rest/v1/catalog_inventory_items?on_conflict=item_key`, { method: 'POST', headers: { apikey: env.SUPABASE_SERVICE_ROLE_KEY, Authorization: `Bearer ${env.SUPABASE_SERVICE_ROLE_KEY}`, 'Content-Type': 'application/json', Prefer: 'resolution=merge-duplicates,return=representation' }, body: JSON.stringify([payload]) }); if (!res.ok) return json({ error: await res.text() }, 500); return json({ ok: true, item: (await res.json())?.[0] || null }); } catch (err) { return json({ error: String(err) }, 500); } } function json(data, status = 200) { return new Response(JSON.stringify(data), { status, headers: { 'content-type': 'application/json; charset=utf-8', 'cache-control': 'no-store' } }); }