import {requireStaffAccess,json} from '../_lib/staff-auth.js';
import {serviceReady,rest,clean} from '../_lib/daip-media.js';
export async function onRequest(context){if(context.request.method!=='GET')return json({ok:false,error:'Method not allowed.'},405);const auth=await requireStaffAccess({request:context.request,env:context.env,capability:'manage_staff',allowLegacyAdminFallback:true});if(!auth.ok)return auth.response;if(!serviceReady(context.env))return json({ok:false,error:'Supabase service configuration is missing.'},503);const id=clean(new URL(context.request.url).searchParams.get('session_id'),80);if(!/^[0-9a-f-]{36}$/i.test(id))return json({ok:false,error:'Valid session_id is required.'},400);try{const session=(await rest(context.env,`daip_media_upload_sessions?select=*&id=eq.${encodeURIComponent(id)}&limit=1`))?.[0];if(!session)return json({ok:false,error:'Upload session not found.'},404);const parts=await rest(context.env,`daip_media_upload_parts?select=part_number,etag,size_bytes,uploaded_at&session_id=eq.${encodeURIComponent(id)}&order=part_number.asc`);return json({ok:true,session,parts:parts||[]});}catch(e){return json({ok:false,error:e.message},500)}}
