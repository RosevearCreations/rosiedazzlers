// Build 185 — list payment applications against deposits, invoices, and refunds.
import { requireStaffAccess, json, serviceHeaders } from "../_lib/staff-auth.js";
export async function onRequestPost({request,env}){try{const body=await request.json().catch(()=>({}));const access=await requireStaffAccess({request,env,body,capability:"manage_bookings",allowLegacyAdminFallback:true});if(!access.ok)return access.response;if(!hasSupabaseConfig(env))return json({ok:true,table_ready:false,applications:[],warning:"Supabase not configured."});const res=await fetch(`${env.SUPABASE_URL}/rest/v1/payment_applications?select=*&order=created_at.desc&limit=${Math.min(200,Number(body.limit||100)||100)}`,{headers:serviceHeaders(env)});const text=await res.text();const data=safeJson(text);if(!res.ok)return json({ok:true,table_ready:false,applications:[],warning:data?.message||text||"payment_applications table is not ready."});return json({ok:true,table_ready:true,applications:Array.isArray(data)?data:[]});}catch(err){return json({ok:false,error:err?.message||"Could not list payment applications."},500)}} function hasSupabaseConfig(env){return !!(env?.SUPABASE_URL&&(env?.SUPABASE_SERVICE_ROLE_KEY||env?.SUPABASE_SERVICE_KEY||env?.SUPABASE_SERVICE_ROLE||env?.SUPABASE_SECRET_KEY));} function safeJson(t){try{return JSON.parse(t)}catch{return null}}
