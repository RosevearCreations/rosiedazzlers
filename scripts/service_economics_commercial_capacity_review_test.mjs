#!/usr/bin/env node
import assert from "node:assert/strict";
import { buildServiceEconomicsCommercialCapacityReview } from "../functions/api/_lib/service-economics-commercial-capacity-review.js";
const result=buildServiceEconomicsCommercialCapacityReview({generated_at:"2026-09-19T22:00:00.000Z",economics:{totals:{booking_count:4,ready_booking_count:2,review_booking_count:1,unavailable_booking_count:1,cogs_variance_booking_count:1,recognized_revenue_cad:1200,recorded_material_cost_cad:180,estimated_direct_labor_cad:300,pricing_review_contribution_cad:720},rows:[{customer_name:"Secret Person",booking_id:"secret-booking"}]},fleet:{inquiry_demand:{inquiry_count:3,vehicles_requested:12},commercial_rules:{unresolved_domain_count:2,commercial_terms_complete:false},operations:{fleet_account_count:1,active_vehicle_count:6,request_job_count:4,completed_work_evidence_count:2},capacity:{status:"unavailable",availability_authority:"/api/availability",collision_revalidation_authority:"/api/checkout",current_live_capacity_inferred:false}},source_status:{service_economics:{available:true,restricted:false,http_status:200},fleet_commercial:{available:true,restricted:false,http_status:200}}});
assert.equal(result.build,443);assert.equal(result.evidence_status,"review");assert.equal(result.economics.evidence_completeness_pct,50);assert.equal(result.economics.pricing_review_contribution_cad,null);assert.equal(result.commercial.signed_business_inferred,false);assert.equal(result.capacity.current_live_capacity_inferred,false);assert.ok(result.review_candidates.some(r=>r.area==="profitability_completeness"));assert.ok(result.review_candidates.some(r=>r.area==="commercial_rules"));assert.ok(result.review_candidates.some(r=>r.area==="live_capacity"));assert.ok(result.review_candidates.every(r=>r.automatic_action_authorized===false));assert.equal(result.boundaries.pricing_mutation_allowed,false);assert.equal(result.boundaries.booking_mutation_allowed,false);assert.equal(result.boundaries.provider_transaction_allowed,false);const serialized=JSON.stringify(result);for(const forbidden of ["Secret Person","secret-booking"])assert.equal(serialized.includes(forbidden),false);
const unavailable=buildServiceEconomicsCommercialCapacityReview({source_status:{service_economics:{available:false,restricted:false,http_status:503},fleet_commercial:{available:false,restricted:false,http_status:503}}});assert.equal(unavailable.evidence_status,"partial");assert.equal(unavailable.economics.pricing_review_contribution_cad,null);console.log("BUILD 443 SERVICE ECONOMICS COMMERCIAL CAPACITY REVIEW TEST: PASS");
